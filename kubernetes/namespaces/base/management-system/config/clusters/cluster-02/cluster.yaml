---
apiVersion: container.cnrm.cloud.google.com/v1beta1
kind: ContainerCluster
metadata:
  name: cluster-02
  namespace: management-system
spec:
  enableAutopilot: true
  location: us-west1
  releaseChannel:
    channel: RAPID
  workloadIdentityConfig:
    workloadPool: kubecon-mgmt.svc.id.goog
---
apiVersion: gkehub.cnrm.cloud.google.com/v1beta1
kind: GKEHubMembership
metadata:
  name: cluster-02
  namespace: management-system
spec:
  location: global
  authority:
    # Issuer must contain a link to a valid JWT issuer. Your ContainerCluster is one. To use it, replace ${PROJECT_ID?} with your project ID.
    issuer: https://container.googleapis.com/v1/projects/kubecon-mgmt/locations/us-west1/clusters/cluster-02
  endpoint:
    gkeCluster:
      resourceRef:
        name: cluster-02
---
apiVersion: notification.toolkit.fluxcd.io/v1beta2
kind: Alert
metadata:
  name: cluster-02
  namespace: flux-system
spec:
  summary: "cluster-02"
  eventSeverity: info
  eventSources:
  - kind: Kustomization
    name: cluster-02
    namespace: flux-system
  providerRef:
    name: flux-system-dispatch
  eventMetadata:
    app.kubernetes.io/project: "kubecon-mgmt"
    app.kubernetes.io/cluster: "cluster-02"
    app.kubernetes.io/location: "us-west1"
